<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Online library</title>
</head>
<body>
<div>
<form action="/logout" method="post">
<input type="hidden" name="_csrf" value="{{_csrf.token}}"/>
<input type="submit" value="Sing Out"/>
</form>
</div>
<div>
        <form method="post">
            <input type="hidden" name="_csrf" value="{{_csrf.token}}">
            <br><input type="text" name="author" placeholder="Введите имя автора">
            <br><input type="text" name="bookName" placeholder="Введите название книги">
            <br><input type="text" name="bookYear" placeholder="Введите год издания книги">
            <br><input type="text" name="bookGenre" placeholder="Введите жанр книги">
            <br><button type="submit">Добавить книгу</button>
        </form>
</div>
    <div>
        <form method="post" action="filter">
            <input type="hidden" name="_csrf" value="{{_csrf.token}}">
            <br><input type="text" name="filter" placeholder="По имени автора">
            <br><button type="submit">Поиск</button>
        </form>
    </div>
    {{#books}}
        <div>
            <b>{{id}}</b>
            <span>{{author}}</span>
            <span>{{bookName}}</span>
            <span>{{bookYear}}</span>
            <span>{{bookGenre}}</span>
            <strong>{{authorName}}</strong>
        </div>
{{/books}}
</body>
</html>